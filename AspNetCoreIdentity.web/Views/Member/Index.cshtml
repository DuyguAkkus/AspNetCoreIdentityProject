@{
    ViewData["Title"] = "Kullanıcı Bilgileri";
}

<h2 class="fw-bold text-center mt-4">Hoşgeldin, @(User.Identity?.IsAuthenticated == true ? User.Identity.Name : "Ziyaretçi")! 🎉</h2>

@if (User.Identity?.IsAuthenticated == true)
{
    <div class="container mt-4">
        <div class="card shadow-lg rounded-4 border-0 custom-card">
            <div class="custom-header card-header text-white text-center rounded-top-4">
                <h4 class="fw-light mb-0">Kullanıcı Bilgileri</h4>
            </div>
            <div class="card-body text-center">
                <!-- PROFİL RESMİ -->
                <div class="profile-picture-container mb-3">
                    <img src="@(string.IsNullOrEmpty(Model.PictureUrl) ? "/userPictures/default_user.jpg" : Model.PictureUrl)"
                         alt="Profil Resmi" class="profile-picture">
                </div>

                <table class="table align-middle custom-table">
                    <tbody>
                    <tr>
                        <td class="fw-bold text-muted text-start"><i class="bi bi-person-circle"></i> Kullanıcı Adı:</td>
                        <td class="text-end">@Model.UserName</td>
                    </tr>
                    <tr>
                        <td class="fw-bold text-muted text-start"><i class="bi bi-envelope"></i> Email:</td>
                        <td class="text-end">@Model.Email</td>
                    </tr>
                    <tr>
                        <td class="fw-bold text-muted text-start"><i class="bi bi-telephone"></i> Telefon:</td>
                        <td class="text-end">@Model.PhoneNumber</td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <div class="alert alert-warning text-center mt-4">
        <strong>⚠ Giriş yapmadınız.</strong> Kullanıcı bilgilerini görmek için lütfen giriş yapın.
    </div>
}

<!-- Özel CSS ile profil resmini ve tabloyu modern hale getiriyoruz -->
<style>
    .custom-card {
        max-width: 600px;
        margin: auto;
        border-radius: 15px;
        overflow: hidden;
        background-color: #ffffff;
    }

    .table {
        border-radius: 10px;
        overflow: hidden;
    }

    .custom-table td {
        padding: 15px 20px;
        font-size: 16px;
        border-bottom: 1px solid #ddd;
    }

    .custom-table tr:last-child td {
        border-bottom: none;
    }

    .table-hover tbody tr:hover {
        background-color: rgba(255, 79, 121, 0.1);
    }

    .fw-light {
        font-weight: 300;
    }

    .rounded-top-4 {
        border-top-left-radius: 15px;
        border-top-right-radius: 15px;
    }

    .custom-header {
        background-color: #ff4f79 !important;
        padding: 15px;
        font-size: 18px;
        font-weight: bold;
    }

    /* PROFİL RESMİ */
    .profile-picture-container {
        display: flex;
        justify-content: center;
    }

    .profile-picture {
        width: 140px;
        height: 140px;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid #ff4f79;
        padding: 5px;
        background-color: #fff;
    }

    /* Responsive tasarım */
    @@media (max-width: 768px) {
    .custom-card {
        width: 90%;
    }

    .custom-table td {
        font-size: 14px;
        padding: 10px;
    }

    .profile-picture {
        width: 100px;
        height: 100px;
    }
    }
</style>
